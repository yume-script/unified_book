# 📚 Unified BOOK Search

BookOasis 미디어 서버에서 코어 수정 없이 메타데이터 검색과 라이브러리 관리를 지원하는 통합 도서 검색 플러그인 패키지입니다. 알라딘, 네이버, 구글의 도서 데이터를 유기적으로 조회하여 신뢰도 높은 도서 정보를 완성합니다.

---

## ✨ 주요 특징 (Key Features)

| 기능 | 설명 |
| :--- | :--- |
| **🔍 3-in-1 통합 검색** | 알라딘, 네이버, 구글 도서 API를 동시에 호출하고 중복 결과를 필터링하여 신속하게 도서를 매칭합니다. |
| **🖼️ 고해상도 커버 수집** | 원본에 가까운 표지 품질 확보를 위해 소스별 이미지 주소를 정제하여 가장 선명한 최고 화질의 커버를 다운로드합니다. |
| **⚡ WebP 이미지 가공** | 다운로드한 표지 이미지를 WebP 포맷(Quality 95)으로 압축 저장하여 디스크 용량을 보존하고 페이지 로딩 속도를 높입니다. |
| **🏷️ 정돈된 UI 레이아웃** | 소개글 본문(Description) 영역에 서지 정보를 중복 노출하여 지저분해지는 현상을 방지하고, 시스템이 출력하는 `출간일` 영역 끝자리에 `[ISBN: ...]` 값을 결합하여 깔끔한 한 줄 라인으로 출력합니다. |
| **🔐 기존 ISBN 데이터 보호** | 신규 검색 결과에 ISBN 정보가 비어 있는 경우에도, 기존 데이터베이스에 저장되어 있던 소중한 ISBN 데이터가 공백으로 덮어씌워져 지워지지 않도록 `COALESCE / NULLIF` 안전 쿼리를 수행합니다. |
| **📂 하이브리드 임포트 지원** | 패키지 형태의 임포트뿐만 아니라 코어가 플러그인 파일을 개별 동적 로드(Dynamic Load)할 때 발생할 수 있는 상대 경로 참조 에러(`ImportError`)를 안전하게 자체 복구합니다. |

---
## 📅 업데이트 내역 (Changelog)

### 🔴 V20260721 (최신 업데이트)
*   **구조 모듈화 진행:** 단일 파일 구조에서 탈피하여 각 검색 소스(`aladin.py`, `naver.py`, `google.py`)와 이미지 처리 및 포맷 유틸리티(`utils.py`)를 독립적인 모듈 구조로 개편하여 장기적인 유지보수성을 향상시켰습니다.
*   **ISBN 데이터베이스 직접 기록 지원:** 최신 BookOasis 스키마의 `books.isbn` 컬럼에 직접 대응하도록 쿼리 구조를 고도화했습니다.
*   **UI 정보 통합 개선:** 소개글 내용에 메타데이터가 난잡하게 섞여서 중복 출력되던 레이아웃 문제를 우회하여 `출간일` 정보 꼬리표 영역에 출력되도록 정리했습니다.
*   **로컬 벌크 업데이트 도구 추가:** CLI 환경에서 라이브러리 폴더 전체의 책들을 검사해 DB를 일괄 갱신하는 통합 스캔 도구(`scan_isbns.py`)를 배포하기 시작했습니다.

### ⚪ V20260715
*   `release_date`, `summary`, `link` 등 BookOasis 최신 스키마 대응.
*   알라딘 `cover500` 요청 적용, 구글 `zoom=3` 가공, 네이버 사이즈 제한 제거 등 커버 화질 개선.
*   설명문(Summary) 내부 불필요한 HTML 태그를 정화하여 깔끔한 텍스트 저장 지원.

---

## 📦 설치 및 적용 가이드 (Installation)

1.  `plugins/metadata/` 경로 하부에 `unified_book` 폴더를 생성하고 패키지 파일들을 배치합니다.
2.  BookOasis 웹 UI의 환경설정 > 플러그인 메뉴에서 `Unified BOOK Search` 활성화 스위치를 켭니다.
3.  알라딘 TTBKey 및 네이버 Client API 값을 설정하고 저장을 누릅니다.
4.  책 상세 정보 수정 > 돋보기 아이콘(검색) 클릭 시 알라딘/네이버/구글의 정돈된 검색 결과를 즉시 조회하실 수 있습니다.
